# Local-only convenience. On Runpod, inject the same key as a secret-backed
# environment variable instead of creating a .env file in the container.
GEMINI_API_KEY=your-gemini-api-key
WORKER_GEMINI_MODEL=gemini-2.5-flash
